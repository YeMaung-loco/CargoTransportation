package Mapper;

import java.sql.ResultSet;
import java.sql.SQLException;

import Model.Customer;

public class Package_Mapper {
	// public static Package mapper(Package orderPackage, ResultSet rs) {

	        /*try {
	        	orderPackage.setNo(rs.getString("package_no"));
	        	orderPackage.setName(rs.getString("c_name"));
	        	orderPackage.setPhone(rs.getString("c_phone"));
	            customer.setAddress(rs.getString("c_address"));
	        } catch (SQLException e) {
	            e.printStackTrace();
	        }
	        return Package;
	    }*/
	// }
	
}
